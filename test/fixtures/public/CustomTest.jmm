import static ioPlus.printHelloWorld(void) void;
import ioPlus(int, int, boolean) int;

class CustomTest extends Test{
    int something;
    Stonk stonk;
    boolean truth;

	public int stonk(int[] args, int nut) {
        int num;
        boolean star;

        args.hello()[5] + 2;
        args[1][2];
        num = 5;
        num = num.length && num.length;

        //if((num < 1 && num < 1)){
        if((2-3+4 && 2*4-6) < !num && (2-3/45 < 34+5) < 4/4*1){
            num = 5;
        } else {
            num = 7;
        }

        while(!num){
            star = true;
            num = !star * 3+2;
        }

        if((2*3-4) < (num+4/2)){
            args[5].hello().hello()[4][4].hello();
            arg[3] && 4;
        } else {
            args[5];
            num = 7*3;
        }

        new int[5];
        new Parser();

        ioPlus.length;

		ioPlus.printHelloWorld(num, num+3*5);

		return num;
	}
}